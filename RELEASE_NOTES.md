## Adventure v1.0.6

目标管理新增「本周」运行时段视图；词汇自走棋改为前排对撞，并加入站位配合与成长词。

完整说明与设计文档：[README.md](README.md) · [docs/README.md](docs/README.md)

### 本版变化

- **本周运行时段**：目标管理新增「本周」Tab；周一到周日七列 × 0–24 点小时格，同时显示顶层目标与当时聚焦的叶子
- **时段记账**：换顶层或换叶子会切段；跨午夜按自然日切开、按自然周查询；图例按身份分行并配色块，便于对照格子颜色
- **独立日志**：运行时段写入 `%APPDATA%\Adventure\runtime_intervals.json`（与主存档分离）；损坏则归档重置；随 tick 记账，存档与退出时写盘
- **词汇自走棋**：战斗改为前排对撞（双方当前前排互打）；加入站位配合与成长词

### 主要功能

- **悬浮窗目标树**：大目标 / 子目标（文件夹）管理，分解、暂停、完成，正在运行的路径高亮
- **本周时段视图**：在目标管理中查看本周真实运行区间（顶层 + 叶子）
- **奖励系统**：按键鼠操作周期性开奖金币与钻石，五档稀有度宝箱，缓动宝箱进度条，开奖历史
- **皇室战争式开箱**：解锁倒计时 + A-Z × 5 稀有度字母收藏与伴生货币
- **小游戏**：小动物竞技场、像素格子战场、词汇自走棋（金币/铜币入场，按关卡结算）
- **音效**（可开关）、托盘常驻、开机自启、固定到所有虚拟桌面
- **数据本地保存**于 `%APPDATA%\Adventure`（每 15 秒自动保存，损坏自动备份）

### 下载与运行（Windows 10/11 x64）

1. 下载本页下方的 `Adventure-v1.0.6.zip` 并解压到任意目录
2. 双击 `Adventure.exe` 即可运行，无需安装 Python
3. 若 Windows SmartScreen 提示，点「更多信息」→「仍要运行」（exe 未签名，属正常现象）
4. 首次启动解压内置依赖，稍慢属正常

### 已知问题

- 未做代码签名，个别杀软可能误报，请添加信任
- 仅支持 Windows x64

### 源码

本页下方的 `Source code (zip)` / `Source code (tar.gz)` 为对应 tag 的完整源码；本地构建可执行 `build.bat`（见 README「打包为 exe」）。

---

## 历史版本

### v1.0.5

- **全局货币口径**：全局金币 / 钻石只统计已放进奖励背包的部分
- **缓动进度条**：点完宝箱后从 0 再开始；修复连续进度条不动的问题

### v1.0.4

- **缓动单箱进度条**：颜色与闪动随宝箱稀有度变化
- **输入计数**：鼠标移动也计数；虚拟机环境下也可计数
- **词汇自走棋**：战斗动画；场内用铜币；保存过往阵型作为敌方
- **布局**：奖励背包与游戏入口过大导致看不到底部内容的问题已收紧
- **音效**：设置里声音可重复播放

### v1.0.3

- **修复**：悬浮窗钻石与金币显示不正对的问题

### v1.0.2

- **开箱系统**：宝箱需解锁倒计时（普通 30 分 → 传奇 8 小时），最多 4 个同时解锁，真实时间存盘（关掉应用照常走）
- **字母收藏**：开箱开出 A-Z × 5 稀有度（共 130 种组合），背包内 26×5 网格展示进度与数量
- **概率分布（全部右偏）**：字母数量几何分布（平均每箱 2 个）；字母稀有度按宝箱稀有度加权；伴生货币金币 50% / 钻石 20%，金额对数正态
- **开箱动画**：宝箱发光晃动 → 字母卡点一张揭一张
- **修复**：宝箱解锁 / 开箱按钮状态切换导致点击无响应的问题

### v1.0.1

- **修复**：打包后音效 / 资源路径错误（PyInstaller 6 资源在 `_internal/`）
- **优化**：只打包实际用到的 Qt 模块，安装包约 639 MB → 164 MB
- 功能与 v1.0.0 一致

### v1.0.0

首个正式版本。无需安装 Python，解压即用。

- **悬浮窗目标树**：大目标 / 子目标管理，分解、暂停、完成，正在运行的路径高亮
- **奖励系统**：按键鼠操作周期性开奖金币与钻石，五档稀有度宝箱，缓动进度条，开奖历史
- **小游戏**：小动物竞技场、像素格子战场
- **音效**、托盘常驻、开机自启、固定到所有虚拟桌面
- **数据本地保存**于 `%APPDATA%\Adventure`
